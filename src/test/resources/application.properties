bot.name=${BOT_NAME}
bot.token=${BOT_TOKEN}
hh.client_id=${CLIENT_ID}
hh.client_secret=${CLIENT_SECRET}

spring.datasource.url=jdbc:postgresql://localhost:5432/test-resume-bot
spring.datasource.username=postgres
spring.datasource.password=300303
spring.datasource.driver-class-name=org.postgresql.Driver

server.address=${SERVER_ADDRESS:localhost}
server.port=${SERVER_PORT:5000}
server.url=http://${server.address}:${server.port}

hh.base-url=https://api.hh.ru

json.max.size.mb=${JSON_MAX_SIZE:16}

spring.jpa.properties.hibernate.default_schema=test-resume-bot
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true
spring.jpa.show-sql=true